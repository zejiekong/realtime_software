//dynamic allocation gives more flexibility during runtime
//static memory stays for the duration of program

/*
malloc:
    malloc() returns pointer of type void
    can use casting operator to convert to char .. 
    if there is space, returns address
    otherwise, NULL
*/

/*
free((void* )...)
*/