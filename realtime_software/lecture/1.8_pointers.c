//pointers contain address of variables
// example: int *p = &i , store address of i



/*
if struct pointer,
modify field
(*p).y = ... or p->y = ... (favoured)
*/

/*
const pointers 
*/

/*
name of array is address, no need for &
*/

#include <stdio.h>

int main()
{
    /*
    int i = 3;
    int *p = &i;
    printf("%d",*p); //*p dereferences p hence gives value of i
    *p = 5; //changes i to 5
    */

}